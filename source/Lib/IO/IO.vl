def puts<T>( value : T ) -> IO::StandardStream
  IO.stdout.puts( value.to_string )
end

def print<T>( value : T ) -> IO::StandardStream
  IO.stdout.print( value.to_string )
end


#{ TODO: Implement assert! macro to include file, line, and column information #}
def assert!( condition : Bool ) -> Void
  if not condition
    raise "ASSERTION FAILED #{__FILE__}:\tCalled by #{__FUNCTION__} at #{__LINE__}:#{__COLUMN__}"
  end
end
