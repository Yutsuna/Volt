# Volt Core — Regex

@[External]
def volt_regex_match( regex : Regex, str : UInt8*, size : UInt64 ) -> Int64

struct Regex
  def ===( other : String ) -> Bool
    volt_regex_match( self, other.data, other.size ) != 0
  end
end
