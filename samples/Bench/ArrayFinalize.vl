def bench_array_finalize -> UInt64

  total = 0

  1Mo.times do
    arr = Array<Int32>.new( 200B )
    50.times { | i | arr << i }
    total += arr.size
  end

  total

end


assert! bench_array_finalize == 50Mo
