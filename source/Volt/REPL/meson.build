#{
# REPL/meson.build
#   ReplDoc       spans, documents, tables, pane
#   ReplSyntax    Volt / LLVM IR / machine code
#   ReplEval      the compile-and-run session
#   ReplQuery     the `:` builtins
#   ReplComplete  candidates for what is half-typed
#   ReplCore      line classification and session history
#   ReplTui       termios, ANSI, editing, the only module allowed to I/O
#}

subdir('ReplDoc')
subdir('ReplSyntax')
subdir('ReplEval')
subdir('ReplQuery')
subdir('ReplComplete')
subdir('ReplCore')
subdir('ReplTui')
