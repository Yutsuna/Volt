def bench_array_finalize -> UInt64
  iter = 0_u64
  total = 0_u64

  while iter < 1Mo
    arr = Array<Int32>.new(50_u64)

    i = 0
    while i < 50
      arr.push(i)
      ++i
    end

    total += arr.size
    ++iter
  end

  total
end


res = bench_array_finalize()
assert!(res == 50Mo)
