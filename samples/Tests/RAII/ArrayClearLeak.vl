def assert!( condition : Bool ) -> Void
  if not condition
    raise "Assertion failed #{__FILE__}:#{__LINE__}"
  end
end

def test_array_clear -> Void
  arr = Array<String>.new
  arr << "hello_leak_test_1".dup
  arr << "hello_leak_test_2".dup
  arr.clear
end

test_array_clear
